// Mock data is no longer used — all data comes from Supabase.
// Kept as empty exports to avoid breaking any stale imports during transition.
export const mockAssets: any[] = [];
export const mockSops: any[] = [];
export const mockRequirements: any[] = [];
